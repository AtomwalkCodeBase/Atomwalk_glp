import { StyleSheet, Text, View } from 'react-native'
import LabActivityScreen from '../../src/screens/LabActivityScreen';

const index = () => {
  return (
	<View style={{ flex: 1}}>
			<LabActivityScreen/>
	</View>
  )
}

export default index

const styles = StyleSheet.create({})