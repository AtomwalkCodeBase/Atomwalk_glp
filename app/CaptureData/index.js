import { StyleSheet, View } from 'react-native'
import CaptureData from '../../src/screens/CaptureData';

const index = () => {
    return (
        <View style={{ flex: 1 }}>
            <CaptureData />
        </View>
    )
}

export default index

const styles = StyleSheet.create({})